window.addEventListener('error', function(errorEvent) {
  var errorInfo = {
    message:     errorEvent.message,
    file:        errorEvent.filename,
    line_number: errorEvent.lineno
  };
  $.post('/_js_exceptions', errorInfo);
});
